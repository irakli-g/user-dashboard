import React from "react";
import { User } from "../../reducer/reducer";

const UserPermissions: React.FC<User> = (props) => {
  return <div className="user-permissions"></div>;
};

export default UserPermissions;
